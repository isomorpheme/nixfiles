- defaults:
    link:
      relink: true

- clean:
  - '~'

# vim
- link:
    ~/.vimrc: vim/.vimrc
    ~/.vim: vim/.vim

- shell:
  - [git submodule update --init --recursive, "installing submodules"]
