- defaults:
    link:
      relink: true

- clean:
  - '~'

- create:
  - ~/.local/bin

# vim
- link:
    ~/.vimrc: vim/.vimrc
    ~/.vim: vim/.vim

- shell:
  - [git submodule update --init --recursive, "installing submodules"]
