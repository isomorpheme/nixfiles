[user]
    name = Daan Rijks
    email = daanrijks@gmail.com
[core]
    autocrlf = input
    safecrlf = true
    askpass = 
[alias]
    ps = push
    pl = pull
    co = checkout
    c = commit
    cm = commit -m
    ce = commit -e
    st = status
    a = add
    ap = add -p
    br = branch
    graph = log --graph --abbrev-commit --decorate \
        --format=format:'%C(bold blue)%h%C(reset) - %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    lgraph = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%ad%C(reset) %C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all --date='format:%y/%-m/%-d %H:%M'
    mkbr = checkout -b
    pull-sub = !git pull --recurse-submodules && git submodule update --recursive
[credential]
    helper = cache
[push]
    default = simple
[diff]
    algorithm = histogram
